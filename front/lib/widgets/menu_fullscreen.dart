import 'package:flutter/material.dart';

class MenuFullScreen extends StatelessWidget {
  const MenuFullScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Column(
          children: [
            Text("menu full screen "),
            Text("menu full screen "),
            Text("menu full screen "),
            Text("menu full screen "),
            Text("menu full screen "),
            Text("menu full screen "),
            Text("menu full screen "),
            Text("menu full screen "),
            Text("menu full screen "),
          ],
        ),
      ),
    );
  }
}
